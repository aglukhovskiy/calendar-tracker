name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # или master, в зависимости от названия вашей основной ветки

permissions:
  contents: write # Разрешение на запись для репозитория
  pages: write # Разрешение на запись для GitHub Pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Clean install dependencies
        run: |
          npm cache clean --force
          rm -rf node_modules
          rm -f package-lock.json
          npm install

      - name: Verify html-webpack-plugin installation
        run: ls -l node_modules/html-webpack-plugin

      - name: Create .env file and expose to GITHUB_ENV
        run: |
          echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" >> $GITHUB_ENV
          echo "SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}" >> $GITHUB_ENV

      - name: Build
        run: npm run build

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist  # папка, куда собирается ваш проект
          branch: gh-pages  # ветка, куда будет деплоиться сайт 